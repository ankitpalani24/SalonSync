{
  "name": "salonflow-ai",
  "version": "1.0.0",
  "description": "Enterprise Salon Management SaaS Platform",
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "npm run dev --prefix frontend",
    "server": "npm start --prefix backend",
    "dev-all": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\"",
    "dev-all-host": "concurrently \"npm run dev --prefix frontend -- --host\" \"npm start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
